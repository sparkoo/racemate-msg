{
  "compilerOptions": {
    "module": "esnext", // Or "node16", "nodenext"
    "target": "esnext", // Or a target that supports ESM
    "moduleResolution": "node", // Or "node16", "nodenext" (important for Node.js ESM)
    "declaration": true
  },
  "include": [
    "proto/*.ts", // Or src/**/*.js if protoc-gen-ts outputs JavaScript
    "proto/*.proto" // Include proto files to be compiled
  ]
}
